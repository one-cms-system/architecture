name: logo
schema:
  title:
    type: string
    uiParams:
      component: input
      inputType: text
      order: 1
  preview-logo:
    multilingual: true
    type: image
    allowedTypes: [ "jpg", "jpeg", "png" ]
    uiParams:
      component: image
      order: 2
  original-logo:
    multilingual: true
    type: archive
    allowedTypes: [ "zip" ]
    uiParams:
      component: file-upload
      order: 2
  logo-rules:
    multilingual: true
    type: pdf
    allowedTypes: [ "pdf" ]
    uiParams:
      component: file-upload
      order: 2
uiParams:
  component: post-table
