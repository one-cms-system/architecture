name: company-experts
schema:
  enabled:
    type: boolean
    uiParams:
      component: checkbox
      order: 2
  name:
    multilingual: true
    type: string
    uiParams:
      component: input
      inputType: text
      order: 1
  surname:
    multilingual: true
    type: string
    uiParams:
      component: input
      inputType: text
      order: 1
  profile-image:
    type: image
    allowedTypes: [ "jpg", "jpeg", "png" ]
    uiParams:
      component: image-upload
      order: 2
uiParams:
  component: post-table
